Bearer Token: UgovWflFf3LBUx

Una vez se tenga PHP instalado en el equipo se procede a ejecutar los scripts

* Que CLI(consola) esté apuntando en la ruta de la carpeta del examen ejemplo: ../Examphp esto para que reconozca y ejecute los archivos API1.php, API2.php*

*API REST 1

Ejecutar el script de la primer API via CLI(consola) con:

comando: php -S localhost:8000 API1.php 

(puede ser el puerto 8000 o el que esté disponible)

# hacer peticiones a la url http://localhost:8000 con Postman
(8000 o el puerto que se haya seleccionado)

Dentro de la carpeta 'Data' se contrara el archivo .txt con los datos.
(si no existe se crea uno)

*API REST 2

Ejecutar el script de la segunda API via CLI(consola) con:

comando: php -S localhost:8000 API2.php 

(puede ser el puerto 8000 o el que esté disponible)

# hacer peticiones a la url http://localhost:8000 con Postman
(8000 o el puerto que se haya seleccionado)


